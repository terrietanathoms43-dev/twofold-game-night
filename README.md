# Twofold Game Night

A TypeScript app for private, synchronized couples game nights.

Copy the environment example to a local environment file and add the Supabase project URL and publishable key.

Run npm install, then npm run dev.
